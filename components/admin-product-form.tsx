"use client";
import { useState } from "react";
export function AdminProductForm({ categories }: { categories: Array<{ id: string; name: string }> }) { const [message, setMessage] = useState(""); return <form className="rounded border bg-white p-6" action={async (form) => { const raw = Object.fromEntries(form); const data = { ...raw, price: Number(raw.price), stock: Number(raw.stock) }; const response = await fetch("/api/admin/products", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(data) }); const result = await response.json(); setMessage(result.success ? "Product created." : result.error.message); }}><h2 className="font-semibold">Create product</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{[["name", "Name"], ["brand", "Brand"], ["price", "Price"], ["stock", "Stock"], ["shortDescription", "Short description"], ["imageUrl", "Image URL"]].map(([name, label]) => <label className="text-sm" key={name}>{label}<input required={name !== "imageUrl"} type={name === "price" || name === "stock" ? "number" : "text"} name={name} className="mt-1 w-full rounded border p-2" /></label>)}<label className="text-sm">Category<select name="categoryId" className="mt-1 w-full rounded border p-2">{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="text-sm sm:col-span-2">Description<textarea required name="description" className="mt-1 min-h-28 w-full rounded border p-2" /></label></div><button className="mt-5 rounded bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Create product</button>{message && <p className="mt-3 text-sm">{message}</p>}</form>; }
