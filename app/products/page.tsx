import Link from "next/link";
import { searchProducts } from "@/lib/services/products";
export const dynamic = "force-dynamic";
export default async function ProductsPage() { const { products } = await searchProducts({ page: 1, limit: 24 }); return <main className="mx-auto max-w-6xl px-6 py-12"><header className="mb-10 flex items-end justify-between"><div><Link href="/" className="text-sm text-slate-500">AgentBridge</Link><h1 className="mt-2 text-4xl font-bold">Products</h1></div><Link href="/cart" className="text-sm font-medium">Cart</Link></header><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <Link className="rounded-lg border bg-white p-5 shadow-sm transition hover:shadow-md" href={`/products/${product.slug}`} key={product.id}><div className="mb-4 aspect-[4/3] rounded bg-slate-100" /><p className="text-sm text-slate-500">{product.brand}</p><h2 className="mt-1 font-semibold">{product.name}</h2><p className="mt-3 font-semibold">${product.price.toFixed(2)}</p></Link>)}</div></main>; }
