import Link from "next/link";
const endpoints = ["POST /api/auth/register", "POST /api/auth/login", "POST /api/auth/logout", "GET /api/auth/session", "GET /api/products/search", "GET /api/products/filter", "GET /api/products/:id", "GET /api/products/recommendations", "GET/DELETE /api/cart", "POST /api/cart/items", "PATCH/DELETE /api/cart/items/:id", "POST /api/cart/coupon", "GET /api/wishlist", "POST /api/wishlist/items", "DELETE /api/wishlist/items/:productId", "GET/POST /api/addresses", "GET /api/orders", "GET /api/orders/:id", "POST /api/orders/:id/cancel", "GET /api/shipping/estimate", "POST /api/checkout"];
export default function ApiDocsPage() { return <main className="mx-auto max-w-4xl px-6 py-12"><Link href="/" className="text-sm text-slate-500">AgentBridge</Link><h1 className="mt-2 text-4xl font-bold">API documentation</h1><p className="mt-4 max-w-2xl text-slate-600">All responses use <code>success</code> and either <code>data</code> or a structured <code>error</code>. Authenticated endpoints derive the user from the HTTP-only session cookie.</p><a href="/openapi.json" className="mt-6 inline-block rounded border bg-white px-4 py-2 text-sm font-medium">Download OpenAPI specification</a><div className="mt-10 rounded border bg-white p-6"><h2 className="font-semibold">Available endpoints</h2><ul className="mt-4 space-y-2 font-mono text-sm text-slate-700">{endpoints.map((endpoint) => <li key={endpoint}>{endpoint}</li>)}</ul></div></main>; }
