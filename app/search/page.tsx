import Link from "next/link";
import { searchProducts } from "@/lib/services/products";
export const dynamic = "force-dynamic";
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q = "" } = await searchParams; const { products, pagination } = await searchProducts({ q, page: 1, limit: 24 }); return <main className="mx-auto max-w-6xl px-6 py-12"><Link href="/" className="text-sm text-slate-500">AgentBridge</Link><h1 className="mt-2 text-4xl font-bold">Search products</h1><form className="mt-8 flex max-w-xl gap-2"><input name="q" defaultValue={q} placeholder="Search products" className="min-w-0 flex-1 rounded border bg-white p-3" /><button className="rounded bg-slate-900 px-5 text-sm font-semibold text-white">Search</button></form><p className="mt-6 text-sm text-slate-600">{pagination.total} result(s){q ? ` for “${q}”` : ""}</p><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <Link key={product.id} className="rounded border bg-white p-5" href={`/products/${product.slug}`}><p className="text-sm text-slate-500">{product.brand}</p><h2 className="mt-1 font-semibold">{product.name}</h2><p className="mt-3 font-semibold">${product.price.toFixed(2)}</p></Link>)}</div></main>; }
