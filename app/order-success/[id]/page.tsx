import Link from "next/link";
export default async function SuccessPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="mx-auto max-w-xl px-6 py-24 text-center"><p className="text-sm font-semibold text-emerald-700">ORDER CONFIRMED</p><h1 className="mt-3 text-4xl font-bold">Thank you for your purchase.</h1><p className="mt-5 text-slate-600">Your order reference is {id}.</p><Link className="mt-8 inline-block rounded bg-slate-900 px-5 py-3 text-sm font-semibold text-white" href={`/account/orders/${id}`}>View order</Link></main>; }
